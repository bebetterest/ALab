Improve `main.py` so the hidden Harbor verifier classifies incident tickets
that need urgent response. Keep the public functions `score_ticket(text)` and
`classify_ticket(text)` available for the verifier.
