# ALab Implementation Dashboard

This file is the short entry dashboard for ALab V1. It answers only three questions: where the project stands, whether completion can be claimed, and which file to read next.

Default read order:

1. `docs/progress.md`: current dashboard only.
2. `docs/progress_pipeline.md`: active batch, active queue, full-suite policy, and update checklist.
3. `docs/completion_audit.md`: requirement-to-evidence matrix and exact proof gaps.
4. `docs/progress_closed_gaps.md`: do-not-reopen guardrails only when a planned batch might duplicate closed work.
5. `docs/progress_log.md`: historical journal only when older context is needed.

The blueprint and subsystem specs remain the normative product contract.

## Current Position - 2026-06-07

ALab has a broad runnable V1 implementation across the local CLI, SQLite home/auth/context foundations, HOME-level agent feedback capture, root-only local read-only dashboard, project/source/experiment lifecycle, local/Docker/Harbor/SkyDiscover runners and adapters, observe/collaboration surfaces, audit, cleanup, and default contract tests.

Current active focus, in summary only: the stable-boundary object-family `services.py` extraction batch and the follow-on low-coupling project lifecycle extraction are now closed for the requested project and experiment families. Auth/root-key/admin-key handlers live in `src/alab/credentials.py`; backup/cache prune handlers live in `src/alab/maintenance.py`; feedback submit/list/show/archive handlers live in `src/alab/feedback.py`; audit list/show handlers and audit object-id filtering live in `src/alab/audit.py`; the dashboard command handler lives with the dashboard server/read models in `src/alab/dashboard.py`; SkyDiscover catalog add/update/show/remove handlers and catalog/adapter-ref resolution helpers live in `src/alab/catalog.py`; annotation add/edit/archive/unarchive/remove plus observe annotation list/show handlers live in `src/alab/annotations.py`; observe run/artifact/log list/show/export/archive/unarchive/remove handlers live in `src/alab/observe.py`; Markdown report export lives in `src/alab/report.py`; source import/list/show/archive/unarchive/remove handlers live in `src/alab/sources.py`; project list/show/archive/unarchive/remove and lock clear-stale handlers live in `src/alab/project_lifecycle.py`; project config/env/secret handlers live in `src/alab/project_config.py`; project validation handlers live in `src/alab/project_validation.py`; experiment list/search/show/best handlers live in `src/alab/experiment_query.py`; experiment archive/unarchive/remove handlers live in `src/alab/experiment_lifecycle.py`; experiment worktree/token/checkout handlers live in `src/alab/experiment_access.py`; shared trash/removal staging lives in `src/alab/removal.py`; shared audit-event writing lives in `src/alab/service_audit.py`; shared request auth gates live in `src/alab/service_auth.py`; and shared text/reason validation lives in `src/alab/service_text.py`. Registered behavior for those CLI surfaces remains unchanged. `src/alab/services.py` now keeps historical `alab.services` imports working through a centralized lazy compatibility export table instead of explicit one-function wrappers for extracted families. Project init, source bootstrap shared with experiment creation, global config, context, run/submit, experiment create/tags, and tightly coupled core lifecycle helpers intentionally remain in `src/alab/services.py` until their helper boundaries can move without fragile cross-module imports. Broad all-at-once service-module extraction and new runner families remain out of scope. Recently closed proof families are tracked in `docs/progress_closed_gaps.md`; do not restart them unless `docs/completion_audit.md` names a new concrete edge or the release target differs from the currently proved host/platform/upstream gates. The detailed queue state lives in `docs/progress_pipeline.md`.

Current annotation behavior also includes optional titles for targeted annotations and title-required targetless current-experiment annotations, with visibility, secret filtering, search, report, dashboard, and experiment-remove cascade coverage.

Current free evaluation behavior also includes paired `runner.type = "none"` and `reward.type = "none"` project configs, `not_required` project validation records, direct experiment submit without evaluator runs, nullable submission `final_run_id`, `final run id: none` rendering, best-ranking exclusion, and lifecycle/report/observe coverage for target experiments that have no final run record.

Current examples now include `examples/free_evaluation_intro_site/`, a runnable free evaluation manual-review task where a worker completes a Simplified Chinese static introduction site and submits directly without `alab run`.

Current dashboard static behavior also includes the 2026-06-07 `0.1.8` presentation fixes: the red best-so-far reward trend series now keeps the prior best value on later non-best runs while the new-best marker/count behavior remains separate, project detail sticky tabs now sit flush against the detail header with an opaque full-width background while scrolling, and run detail KPI rows reserve scrollbar space so metric notes are not clipped. Current CI release behavior also starts with a version synchronization gate that checks `pyproject.toml`, `uv.lock`, `src/alab/__init__.py`, `CHANGELOG.md`, and `CHANGELOG_cn.md` before lint, tests, opt-in gates, or publish jobs run.

The default/local runnable V1 implementation remains broad, and the default suite has been rerun during the 2026-06-07 `0.1.8` dashboard static release review. There is no known missing implementation in the current evidence ledger:

- `docs/completion_audit.md` has current evidence rows for the dashboard static presentation edges, free evaluation mode edge, and no known active `PARTIAL`, `PENDING`, or `ENV-GATED` V1 requirement row outside the status legend and future-state instructions.
- The latest full default-suite closeout gate passed during the 2026-06-07 `0.1.8` dashboard static and CI version-sync review with elevated `.venv/bin/python -m pytest -q`. The sandbox full-suite run still fails only because dashboard tests bind loopback ports that the managed sandbox rejects with `PermissionError`; elevated full default-suite validation remains the completion-quality gate for this local worktree.
- Real Docker-backed Docker/Harbor/SkyDiscover Docker behavior, real Docker capability refresh, SkyDiscover Python local-wheel/network/native dependency behavior, and live SkyDiscover catalog behavior have current opt-in validation on this Darwin/Docker Desktop worktree and current network. The 2026-06-07 skipped-test opt-in subset passed after enabling `ALAB_RUN_REAL_DOCKER`, `ALAB_RUN_LIVE_SKYDISCOVER_CATALOG`, `ALAB_RUN_REAL_SKYDISCOVER_PYTHON`, `ALAB_RUN_NETWORKED_SKYDISCOVER_PYTHON`, and `ALAB_RUN_NATIVE_SKYDISCOVER_PYTHON`; the earlier all-opt-in full suite passed with JUnit `tests=390`, `skipped=0`, `failures=0`, and `errors=0`.
- Focused free evaluation migration/schema/CLI smoke checks, project config import/validation checks, CLI docs contract checks, full `ruff`, forced `compileall`, `git diff --check`, sandbox full-suite dashboard-bind failure confirmation, and elevated full default suite passed for the free evaluation batch. Focused feedback lifecycle, auth re-export, CLI registry/spec/option/static contract, documentation, dashboard, and full default-suite checks passed for the feedback lifecycle batch; focused feedback/auth/contract checks, `compileall`, docs checks, and elevated full default suite passed for the feedback extraction; focused audit/registry contract checks, import-order checks, `compileall`, docs checks, and elevated full default suite passed for the audit extraction; focused dashboard socket/static/registry contract checks, import-order checks, docs checks, and elevated full default suite passed for the dashboard extraction; focused catalog lifecycle/ref/blocker checks, migration catalog/cache contract checks, full relevant ruff checks, registry-derived CLI contract checks, `compileall`, and elevated full default suite passed for the catalog extraction; focused annotation/observe/report/source contract and smoke checks, full relevant ruff checks, `compileall`, sandbox full-suite dashboard-bind failure confirmation, and elevated full default suite passed for that extraction batch; focused removal/trash, auth/key, and maintenance lifecycle tests, full relevant ruff checks, `compileall`, docs checks, sandbox full-suite dashboard-bind failure confirmation, and elevated full default suite passed for the helper/credentials/maintenance extraction; focused project config/env/secret, project validation, experiment query/observe aliases, experiment archive/remove, worktree/token/checkout, project lifecycle/remove, registry-derived parser/option/typed-value, ruff, `compileall`, docs, and full default-suite checks passed for the stable-boundary project/experiment extraction.
- Focused example validation passed for the new free evaluation intro-site example: script syntax checks, dry-run checks, a local setup/demo pass proving `not_required` validation and direct submission with `final run id: none`, focused examples/docs contract tests, and `git diff --check`.
- Focused dashboard static and CI validation passed for the 2026-06-07 fixes: dashboard static frontend test, version synchronization script and CI ordering tests, release-note extraction test with dynamic current-version changelog section comparison, focused `ruff`, full-suite skip listing, opt-in skipped-test subset, and in-app Browser verification that `SkyDiscover Circle Packing` #3 carries #2's best value forward, project detail tabs attach to the header without a translucent gap while scrolling, and the `Embedding reranker` run detail KPI cards remain fully visible above the horizontal scrollbar.

## Gate Snapshot

| Gate | State | Completion blocker |
| --- | --- | --- |
| Requirement evidence | `PASSED` for the current free evaluation mode, feedback, audit, dashboard, catalog, annotation, observe, report, source, credentials, maintenance, project lifecycle, project config, project validation, experiment query, experiment lifecycle, experiment access service extraction, lazy compatibility-export cleanup, and removal helper extraction edges | Reopen only when a changed requirement or environment creates a named evidence gap in `docs/completion_audit.md`. |
| Full default suite | `PASSED` | Latest full pass was the 2026-06-07 elevated `0.1.8` dashboard static and CI version-sync review. Re-run after further implementation/test changes or before a later release-quality claim. |
| CLI contract completeness | focused docs/typed-value checks `PASSED` | Current registered CLI surfaces are proved by generated parser/capability/output matrices, docs-derived success schemas, saved result-failure/system-error checks, and the completion-audit consistency guard; rerun/update when commands or output variants change. |
| Non-CLI hardening | Feedback lifecycle no-SQLite/audit checks and dashboard focused tests `PASSED` | Reopen only if `docs/completion_audit.md` names a concrete non-CLI edge or a release target differs from the currently proved host/platform/upstream gates. |
| Real-environment runners | Docker-backed, real Docker capability-refresh, live catalog, and SkyDiscover Python dependency subsets `PASSED` with `skipped=0` in the all-opt-in suite | Re-run opt-in gates on release-target hosts if host/platform/Python/network/upstream catalog behavior changes. |
| Documentation consistency | focused docs/CLI contract checks `PASSED` | Rerun broader docs checks before a release claim. |

## Do-Not-Reopen Summary

Detailed closed-gap guardrails live in `docs/progress_closed_gaps.md`, not in this dashboard or the active queue. Do not read that file on every batch; open it only when the planned work resembles a previously closed family.

Current high-risk closed families include storage/audit/object retained-relationship proof, core successful workflow proof, CLI golden/command-contract completeness, capability/help/payload preflight proof, root-only local dashboard server/API/frontend proof, runtime stack/architecture proof, host-support policy proof, documentation consistency proof, source/public experiment direction proof, runner/adapter direction proof, current Darwin real Docker-backed runner and capability-refresh validation, current live SkyDiscover catalog validation, runtime/layout guards, V1 plaintext/security-boundary negative proof, parser/preflight/output contracts, lifecycle archive/unarchive/remove evidence mapping, context marker conflict/alias and context repair pinned-commit/old-path blocker guardrails, credential model and credential/token side-effect guardrails, visibility/observe/annotation surfaces, experiment and observe-object list filter/sort matrices, service object-family extraction guardrails, public `--from-exp` visibility-intersection behavior, source import canonical tree-hash/remote-Git fidelity, exp-create source-binding/default-source behavior, run/submit lifecycle behavior, public `--source-git` credential-helper warning behavior, explicit token/inspection observe visibility joins, hard-remove blockers, source/validation hard-remove audit/reference relationships, project/experiment retained-row hard-remove relationships, maintenance and credential audit metadata, artifact/log/reward capture, project config/schema validation and edit semantics, project init precedence, shared-runner cleanup, and Harbor/SkyDiscover catalog/source/evaluator default-path behavior.

## Next Step

No active implementation queue remains for this worktree. Run the full default suite again after any further implementation/test changes or before a later release-quality claim.
