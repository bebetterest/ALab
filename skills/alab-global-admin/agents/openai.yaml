interface:
  display_name: "ALab Global Admin"
  short_description: "Administer ALab homes and root operations"
  default_prompt: "Use $alab-global-admin to initialize or maintain an ALab home, manage root-level resources, and hand project admin access to controllers safely."

policy:
  allow_implicit_invocation: true
