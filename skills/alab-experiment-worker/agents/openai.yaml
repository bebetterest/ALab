interface:
  display_name: "ALab Experiment Worker"
  short_description: "Run and submit ALab experiment worktrees"
  default_prompt: "Use $alab-experiment-worker to improve the current ALab experiment worktree, run one evaluation, and report the run evidence."

policy:
  allow_implicit_invocation: true
