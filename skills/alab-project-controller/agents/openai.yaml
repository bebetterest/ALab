interface:
  display_name: "ALab Project Controller"
  short_description: "Coordinate ALab projects and workers"
  default_prompt: "Use $alab-project-controller to create ALab experiments, launch token-only workers, and summarize project-visible run evidence."

policy:
  allow_implicit_invocation: true
